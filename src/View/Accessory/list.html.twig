{% extends 'layout.html.twig' %}

{% block content %}
    <div class="main-container">
        <h1>My accessories</h1>
        <ul>
        {% for accessory in accessories %}
        <li><img src="{{ accessory.url }}" width="80" height="80" alt="{{ accessory.name }}"></br>{{ accessory.name }}</li>
        {% endfor %}
        </ul>
        <a href="/accessory/add" class="form-btn">retour a la creation</a>
    </div>
{% endblock %}
